# VEX Tracking

Create feature-specific VEX entries only when a dependency advisory impacts shipped artifacts.

For `023-efcore-persistence-adapter`, no active advisory requiring a VEX statement is currently tracked.
