# Completion Transition Release Notes

Completion transitions add first-class completion-triggered routing for ordinary hierarchical composites and parallel composites without introducing artificial user events. Release validation must include focused Core runtime, validation, graph export, visualization rendering, public API snapshot, formatting, full test, and package creation checks before publication.
